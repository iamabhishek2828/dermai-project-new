import React from 'react';

const ConnectDoctor = () => (
  <div>
    <h1>Connect with a Doctor</h1>
    <p>Get connected with remote doctors for expert advice.</p>
  </div>
);

export default ConnectDoctor;
